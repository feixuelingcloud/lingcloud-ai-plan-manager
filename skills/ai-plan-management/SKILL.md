---
name: ai_plan_management
description: 使用GotoPlan工具创建、查询和管理计划。支持智能识别用户意图、自动提取参数,记忆对话上下文。
metadata:
  version: "1.0.1"
  author: "Lingyun"
  openclaw:
    requires:
      config:
        - AI_PLAN_API_KEY
    # ⚠️ 注意：插件同时支持以下三种 Key 名称绑定方式，任意一种有值即可：
    #   1. apiKey            ← OpenClaw 插件配置界面（推荐，claw-hub.json schema）
    #   2. AI_PLAN_API_KEY   ← OpenClaw Skill 配置界面（SKILL.md requires.config）
    #   3. 环境变量 AI_PLAN_API_KEY（OpenClaw 技能系统自动注入）
    # 如果在 OpenClaw 插件设置页面看到的字段名是 "apiKey"，请填写到该字段。
---

# GotoPlan Skill

当用户表达以下需求时,优先调用本技能提供的工具:
- 创建计划
- 查询计划
- 查看计划详情
- 更新任务状�?
- 生成执行报告

## 🎯 核心原则

1. **两步式创�?*: 先生成草稿预�?用户确认后再正式创建
2. **上下文记�?*: 记住最近查询的计划列表,支持"第二个计�?这样的模糊引�?
3. **智能参数提取**: 自动从用户输入中提取时间、分类等信息
4. **友好错误处理**: 遇到错误时给出清晰的建议

---

## 📋 意图识别规则

### 1. 创建计划(两步式流�?

#### 第一�? 生成计划草稿

**触发条件** - 用户�?
- "帮我做一个计�?
- "帮我制定XX计划"
- "生成一个XX计划"
- "创建XX计划"
- "我想做一个XX计划"
- "帮我规划XX"

**操作流程**:
1. 提取关键信息:
   - `goal`: 计划目标/标题(必需)
   - `constraints`: 约束条件/补充说明(可�?
   - `plan_type`: 计划类型(可�?�? work, study, exercise�?
   - `granularity`: 时间颗粒�?可�?默认day)
   - `language`: 语言(可�?默认zh-CN)

2. 调用 `create_plan_draft` 工具生成草稿

3. 将草稿内容完整展示给用户,并明确提�?
   ```
   💡 接下来请确认:
     �?如果没有问题,请输�?"确认" �?"yes" 来正式创�?
     �?如果需要修�?请告诉我需要调整的内容
     �?如果不需�?请输�?"取消"
   ```

4. **重要**: 在对话上下文中记�?`draft_id`,以便后续确认

**参数提取示例**:

| 用户输入 | 提取的参�?|
|---------|----------|
| "帮我制定一�?0天减肥计�? | goal="30天减肥计�?, plan_type="exercise" |
| "帮我做一个工作计�?需要在月底前完成项目A" | goal="工作计划", constraints="月底前完成项目A", plan_type="work" |
| "我想学习Python,每天1小时" | goal="学习Python", constraints="每天1小时", plan_type="study" |

#### 第二�? 确认创建

**触发条件** - 用户在收到草稿后�?
- "确认"
- "yes"
- "好的"
- "没问�?
- "可以"
- "就这�?

**前提条件**: 对话上下文中必须�?`draft_id`(来自第一�?

**操作流程**:
1. 从上下文中提�?`draft_id`
2. 调用 `confirm_plan` 工具,传入 `draft_id`
3. 返回成功消息�?`plan_id`
4. **清除上下文中�?`draft_id`**(避免重复确认)

**错误处理**:
- 如果没有 `draft_id`: 提示用户先生成草�?
- 如果草稿已过�?30分钟): 提示用户重新生成

#### 修改流程

**触发条件** - 用户在收到草稿后�?
- "修改一�?
- "调整XX"
- "改成XX"
- "XX应该改成XX"

**操作流程**:
1. 理解用户要修改的内容
2. 重新调用 `create_plan_draft`,根据用户反馈调整参数
3. 展示新的草稿
4. 更新上下文中�?`draft_id`

---

### 2. 查询计划列表

**触发条件** - 用户�?
- "我有哪些计划"
- "列出我的计划"
- "看看我的计划"
- "告诉我近期的工作计划" �?默认显示本周
- "本周有什么安�?
- "本月有什么计�?
- "查询进行中的计划"

**�?重要: 默认行为**
- **当用户说 "我有哪些计划" / "近期计划" 等模糊查询时,默认显示本周的计�?*
- 除非用户明确指定了其他时间范�?�?"本月" / "全部")

**操作流程**:
1. 提取查询条件:
   - `time_range`: 时间范围 (week=本周/month=本月/all=全部,**默认week**)
   - `status`: 状态筛�?pending/in_progress/completed/blocked)
   - `keyword`: 关键词搜�?

2. 调用 `list_plans` 工具

3. 格式化输出计划列�?
   ```
   📋 您当前有 3 个计�?

   1. 早上晨跑 (运动) �?
      �?截止: 2026-03-30 07:00
      �?进度: 0%

   2. 上午项目会议 (工作) 🔄
      �?截止: 2026-03-30 11:00
      �?进度: 50%

   3. 准备年度报告 (工作) �?
      �?截止: 2026-03-31
      �?进度: 100%
   ```

4. **重要**: 在上下文中保�?`recent_plans` 数组:
   ```javascript
   recent_plans = [
     { index: 1, plan_id: "plan_abc", title: "早上晨跑" },
     { index: 2, plan_id: "plan_def", title: "上午项目会议" },
     { index: 3, plan_id: "plan_ghi", title: "准备年度报告" }
   ]
   ```

**时间范围提取规则**:

| 用户输入 | time_range参数 | 说明 |
|---------|---------------|------|
| "我有哪些计划" / "近期计划" | week | **默认本周** |
| "告诉我近期的工作计划" | week | **默认本周** |
| "本周有什么安�? | week | 本周(周一到周�? |
| "本月的计�? | month | 本月(1日到月底) |
| "全部计划" / "所有计�? | all | 全部时间范围 |

**智能引导**:
- 如果本周无计�?提示用户 "查看本月计划" �?"查看全部计划"
- 如果用户查看本周�?提示可以 "查看本月计划"

---

### 3. 查看计划详情(智能引用)

**触发条件** - 用户�?
- "打开XX计划"
- "看看XX计划"
- "查看XX的详�?
- "告诉我第X个计划的详情" �?重点
- "第X个是什�? �?重点
- "详细说说第X�? �?重点

**智能引用处理** - 这是关键功能!

#### 情况A: 用户使用序号引用

**示例对话**:
```
用户: "我有哪些计划?"
AI: [显示列表,保存 recent_plans 到上下文]

用户: "告诉我第2个计划的详情"
```

**处理流程**:
1. 识别用户使用了序号引�?"�?�?)
2. 检查上下文中是否有 `recent_plans`
3. 如果�?�?`recent_plans` 中提取索引为2�?`plan_id`
4. 调用 `get_plan_detail` 工具,传入提取到的 `plan_id`

**序号提取规则**:
- "第一�? / "�?�? / "1" �?index = 1
- "第二�? / "�?�? / "2" �?index = 2
- "第三�? / "�?�? / "3" �?index = 3
- "最后一�? �?index = recent_plans.length

**错误处理**:
- 如果上下文中没有 `recent_plans`: 提示用户先查询计划列�?
- 如果序号超出范围: 提示用户序号无效,共有X个计�?

#### 情况B: 用户使用计划名称引用

**示例对话**:
```
用户: "打开我的减肥计划"
```

**处理流程**:
1. 先检查上下文中的 `recent_plans`,尝试模糊匹配标题
2. 如果找到匹配,直接使用对应�?`plan_id`
3. 如果没找�?
   - 先调�?`list_plans` 查询包含关键词的计划
   - 如果只有1个结�?直接使用
   - 如果有多个结�?让用户选择

#### 情况C: 用户直接提供 plan_id

如果用户明确提供�?`plan_id`,直接使用�?

---

### 4. 更新计划/任务状态

> ⚠️ **字段区分关键规则（必须严格遵守）**
> - **计划完成状态**  调用 update_plan 工具，使用 completed 布尔字段（	rue/alse）
> - **子任务状态**  调用 update_task_status 工具，使用 status 字符串字段（"done"/"doing" 等）
> - **⛔ 绝对禁止**：调用 update_plan 时传入 status 字段！status 是只读展示字段，后端由 completed 自动计算，手动设置 status **无效且错误**。

---

#### A. 更新计划完成状态（使用 update_plan + completed 字段）

**触发条件** - 用户说：
- "把XX计划标记为完成"
- "完成XX计划"
- "XX计划完成了"
- "标记计划XX为完成"
- "把XX计划设为未完成" / "重新打开XX计划"

**操作流程**:
1. 识别目标计划:
   - 如果用户说"第X个计划"，从 ecent_plans 中提取
   - 如果用户说计划名称，进行模糊匹配
   - 如果不明确，要求用户确认

2. **⛔ 严格要求**：调用 update_plan 工具时，**只能使用 completed 字段**，**禁止使用 status 字段**:
   - "完成" / "完成了"  completed = true
   - "未完成" / "重新打开"  completed = false

   > 原因：status 字段是只读展示字段（由后端根据 completed 自动同步），直接修改 status 不会改变计划的实际完成状态，界面显示也不会变化。

3. **安全确认**: 在执行前复述操作:
   `
   您要将计划"30天减肥计划"标记为已完成，是否确认? (yes/no)
   `

4. 用户确认后，调用 update_plan 工具，**只传入**:
   `json
   {
     "plan_id": "xxx",
     "completed": true
   }
   `

   ❌ **错误示例（绝对不要这样做）**:
   `json
   {
     "plan_id": "xxx",
     "status": "completed"
   }
   `

---

#### B. 更新子任务状态（使用 update_task_status + status 字段）

> ℹ️ **注意**：本节的 status 字段属于 update_task_status 工具的子任务状态，与计划的 completed 字段**完全不同**，不要混淆。

**触发条件** - 用户说（对象是"任务"而非"计划"）：
- "把XX任务标记为完成"
- "开始做XX任务"
- "XX任务完成了"
- "延期XX任务"

**操作流程**:
1. 识别目标任务:
   - 如果用户说"第X个任务"，从计划详情中提取
   - 如果用户说任务名称，进行模糊匹配
   - 如果不明确，要求用户确认

2. 提取目标状态（此处的 status 仅用于 update_task_status 工具，与计划的 completed 无关）:
   - "完成" / "完成了"  status = "done"
   - "开始做" / "开始"  status = "doing"
   - "延期" / "推迟"  status = "delayed"
   - "阻塞" / "卡住了"  status = "blocked"

3. **安全确认**: 在执行前复述操作:
   `
   您要将"上午项目会议"任务标记为已完成，是否确认? (yes/no)
   `

4. 用户确认后，调用 update_task_status 工具

---

### 5. 生成执行报告

**触发条件** - 用户�?
- "生成周报"
- "生成报告"
- "总结执行情况"
- "看看完成�?
- "这个月完成了多少"

**操作流程**:
1. 提取时间范围:
   - "本周" / "这周" �?range = "week"
   - "本月" / "这个�? �?range = "month"
   - 默认: range = "all"

2. 调用 `generate_execution_report` 工具

3. 结构化输�?
   ```
   📊 执行报告

   完成�? 75% (3/4)
   逾期任务: 1
   阻塞任务: 0

   💡 建议:
   �?XX任务已逾期,建议尽快处理
   �?YY任务进展顺利,继续保持
   ```

---

## 🧠 上下文管理规�?

### 需要保存到上下文的数据

1. **draft_id** (草稿ID)
   - 来源: `create_plan_draft` 工具返回
   - 用�? 用户确认时传�?`confirm_plan`
   - 生命周期: 确认创建后清�?�?0分钟后自动清�?

2. **recent_plans** (最近查询的计划列表)
   - 来源: `list_plans` 工具返回
   - 格式:
     ```javascript
     [
       { index: 1, plan_id: "plan_abc", title: "计划A" },
       { index: 2, plan_id: "plan_def", title: "计划B" },
       ...
     ]
     ```
   - 用�? 支持"第X个计�?的引�?
   - 生命周期: 查询新列表时覆盖,�?5分钟后清�?

3. **last_operation** (最后一次操�?
   - 格式:
     ```javascript
     {
       tool: "create_plan_draft",
       params: {...},
       result: {...},
       timestamp: 1711712400000
     }
     ```
   - 用�? 支持"再来一个类似的"等操�?
   - 生命周期: 10分钟后清�?

### 上下文清理规�?

- `draft_id`: 确认创建后立即清�?
- `recent_plans`: 15分钟无操作后清除
- `last_operation`: 10分钟后清�?

---

## 🛡�?安全规则

### 1. 写操作保�?

对以下写操作,执行前必须复述确�?
- `confirm_plan`: "您要创建计划"XX",是否确认?"
- `update_task_status`: "您要�?XX"标记�?XX",是否确认?"

### 2. 对象识别不清处理

当无法确定用户指的是哪个计划�?

**错误示例**:
```
用户: "打开我的计划"
AI: �?直接猜测
```

**正确做法**:
```
用户: "打开我的计划"
AI: 您有多个计划,请问您要查看哪一�?
    1. 30天减肥计�?
    2. 学习Python
    3. 准备年度报告
```

### 3. 输出格式要求

优先使用结构化输�?
- 标题(带emoji)
- 当前状�?
- 关键信息
- 风险提醒(如有)
- 下一步建�?

### 4. 错误处理

工具返回错误�?
1. 先解释错误原�?
2. 给出下一步建�?
3. 必要时主动调用其他工具辅�?

**示例**:
```
�?草稿不存在或已过�?

草稿的有效期�?0分钟。如果超�?0分钟未确�?草稿会自动删除�?

💡 建议: 我可以帮您重新生成计划草�?需要吗?
```

### 5. 避免重复调用

不要在同一轮对话中连续调用相同的写操作�?

---

## 💡 智能增强功能

### 1. 自动补全缺失信息

如果用户创建计划时信息不完整,主动询问:

```
用户: "帮我做一个计�?
AI: 好的,我需要了解一些信�?
    �?这是什么类型的计划?(工作/学习/运动/生活�?
    �?计划的主要目标是什�?
    �?有截止日期吗?
```

### 2. 智能时间推断

用户输入模糊时间�?自动推断:
- "明天" �?计算明天的日�?
- "下周�? �?计算下周五的日期
- "月底" �?计算本月最后一�?

这个功能由后�?`planSystemPrompt.js` 自动处理�?

### 3. 相似计划推荐

创建计划�?如果检测到用户有相似的历史计划,主动提示:

```
�?计划创建成功!

💡 提示: 我发现您之前创建过类似的计划"XX",需要参考吗?
```

### 4. 进度追踪提醒

查询计划�?主动提醒:
- 即将到期的计�?
- 长时间未更新的计�?
- 阻塞的计�?

---

## 📊 对话示例

### 完整流程示例

```
用户: "帮我制定一�?0天减肥计�?每天运动30分钟"

AI: 📋 计划草稿已生�?请仔细确�?

    【计划名称�?0天减肥计�?
    【计划内容】每天运�?0分钟,均衡饮食,规律作息
    【开始日期�?026-03-29
    【截止日期�?026-04-28
    【计划分类】运�?
    【重复频率】单�?每日
    【计划重要性�?
    【计划紧急性�?
    ...

    💡 接下来请确认:
      �?如果没有问题,请输�?"确认" �?"yes" 来正式创�?
      �?如果需要修�?请告诉我需要调整的内容

    �?草稿有效�? 30 分钟
    [上下文保�? draft_id = "draft_xxx"]

用户: "确认"

AI: [从上下文提取 draft_id = "draft_xxx"]
    [调用 confirm_plan 工具]
    �?计划创建成功!

    📋 30天减肥计�?
    🆔 计划ID: plan_abc123
    [清除上下文中�?draft_id]

用户: "我有哪些计划?"

AI: [调用 list_plans 工具]
    📋 您当前有 3 个计�?

    1. 30天减肥计�?(运动) �?
    2. 学习Python (学习) 🔄
    3. 准备年度报告 (工作) �?

    [上下文保�? recent_plans = [
      {index: 1, plan_id: "plan_abc123", title: "30天减肥计�?},
      {index: 2, plan_id: "plan_def456", title: "学习Python"},
      {index: 3, plan_id: "plan_ghi789", title: "准备年度报告"}
    ]]

用户: "告诉我第1个计划的详情"

AI: [从上下文提取 recent_plans[0].plan_id = "plan_abc123"]
    [调用 get_plan_detail 工具,传入 plan_id = "plan_abc123"]

    📋 30天减肥计�?

    📊 基本信息:
      �?状�? 进行�?
      �?进度: ████████░░░░ 45%
      �?分类: 运动

    📅 时间安排:
      �?开�? 2026-03-29 06:00
      �?截止: 2026-04-28 22:00

    📝 描述:
      每天运动30分钟,均衡饮食,规律作息

    💡 建议行动:
      �?距离截止日期还有 15 �?请保持进�?
```

---

## 🔧 调试模式

当环境变�?`DEBUG=true` �?输出详细的调试信�?
- 识别到的意图
- 提取的参�?
- 上下文状�?
- 工具调用结果

---

## 📚 扩展功能(TODO)

未来可以添加的功�?
- 批量操作(批量标记完成)
- 计划模板(快速创建相似计�?
- 团队协作(共享计划)
- 智能提醒(到期提醒)
- 数据分析(完成率趋�?

---

**最后更�?*: 2026-03-29
**版本**: 1.0.0
**维护�?*: Lingyun Team
